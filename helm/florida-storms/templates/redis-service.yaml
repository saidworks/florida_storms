{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "florida-storms.fullname" . }}-redis
  labels:
    {{- include "florida-storms.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.redis.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "florida-storms.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end }}
